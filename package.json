{
  "name": "break-away-seams",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile break-away",
    "test": "bun test"
  },
  "dependencies": {
    "openai": "^4.0.0"
  }
}
